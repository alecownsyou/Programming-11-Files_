print("This program calculates the area of a trapazoid")
height = input("height:")
height = float(height)
bottom_base = input("bottom_base:")
bottom_base = float(bottom_base)
top_base = input("top_base:")
top_base = float(top_base)
area = (bottom_base + top_base) * height / 2
print(area)